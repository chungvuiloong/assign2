import React from 'react';
import ReactDOM from 'react-dom/client';

const Nav = () => {
    return (
        <nav>
                
        </nav>
    );
};

export default Nav;

const root = ReactDOM.createRoot(document.querySelector('nav'));
root.render(
    <Nav />
);